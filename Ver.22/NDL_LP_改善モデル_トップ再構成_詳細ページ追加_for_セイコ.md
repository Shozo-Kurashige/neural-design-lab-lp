# NDL公式LP「改善モデル」セクション再構成＋詳細ページ追加

**注記**：以前渡した「現場改善の一例」単独セクション実装指示は、本プロンプトで置き換えます。トップページ上の詳細表示（Before/After比較、3ステップ、見積Excelの長い説明）はカード2件へ縮小し、詳細は個別ページへ移してください。

## Step 0：着手前に確認・報告すること

コードを書く前に、以下を確認し**短く報告**してください。承認前に実装を始めないこと。

1. `home.tsx` 内で、現在の改善モデルセクション（Before/After・3ステップ）がどこに実装されているか
2. 既存のカード・セクション・アニメーションで再利用できるもの
3. 現在のルーティング方式
4. `president-excel.tsx` を追加する位置
5. `for-construction.tsx` を変更せず利用できるか
6. 変更予定ファイル一覧
7. 影響範囲

---

## 目的

トップページの改善モデル表示を「入口」に限定し、詳細は個別ページへ分離します。トップページで直感的に伝えるべきこと：

- NDLは現場の知恵や経験を会社の資産へ変える
- 改善対象は見積業務だけではない
- 複数の改善モデルがある
- 詳細は個別ページで確認できる

## 全体方針

トップページの以下の要素は、**改善モデルカード2件**へ置き換えてください。

- Before/Afterの詳細比較
- 「整理する・共有する・継承する」の3ステップ
- 見積Excelについての長い説明

トップページでは概要と詳細ページへの導線のみ。既存LP（Hero/Challenges/Approach/Philosophy）のデザイン・余白・タイポグラフィ・色・アニメーション・コンポーネント構成を踏襲し、新しいデザインシステムは作らないこと。

---

## 親セクション（トップページ）

- 内部識別名：`transformation-example-section`
- 表示ラベル：`改善モデル`（既存ルールに合えば英字併記 `TRANSFORMATION MODEL` も可）
- 見出し：`現場の知恵を、会社の資産に変える。`
- サブコピー：`誰かしか使えない現場の知見を、チームで動かせる資産へ。`
- 末尾メッセージ（2カードを束ねる結論として残す）：
```
ツールを入れることが、ゴールではありません。

現場にある知恵を整理し、誰か一人に頼らなくても、
業務と判断が無理なく続いていく状態をつくります。
```

## 改善モデルカード（2件）

PC：2カラム／スマホ：縦並び。カード全体をクリック可能に（または明確なリンク領域）。静かで上品な見た目とし、強いボタン表現・派手なホバーは避ける。2カードは同じレイアウト・同じ高さ・同じ情報構造。

### カード01：president-excel-section

- 表示ラベル：`改善モデル 01`
- 見出し：`属人化した見積業務`
- 本文：`社長だけが扱える見積Excelを、チームで使え、判断を残せる仕組みへ。`
- リンク文言：`詳しく見る`
- リンク先：`/president-excel`
- 詳細ページ：新規ファイル `src/pages/president-excel.tsx`（命名は既存規則＝小文字＋ハイフンに合わせる）

### カード02：for-construction-section

- 表示ラベル：`改善モデル 02`
- 見出し：`散在する現場情報`
- 本文（叩き台。**最終文言は `src/pages/for-construction.tsx` の現在の内容を実際に読んで、そこに忠実な表現へ調整**すること。以下は方向性の参考に留める）：
```
報告書・安全書類・写真・連絡を整理し、
現場の負担を軽くする仕組みへ。
```
含めるべき要素：報告書／安全書類／現場写真／連絡／見積まわり／現場の負担軽減
- リンク文言：`詳しく見る`
- リンク先：`/for-construction`
- 詳細ページ：既存の `src/pages/for-construction.tsx` をそのまま利用。デザイン変更は不可。リンク先として正しく表示するための最小限の調整のみ許可。

---

## フォルダ構成

既存方針（各ページを `src/pages` 直下に配置）を維持。新しい `transformation` ディレクトリ等は作らない。

```
src/
├─ components/
├─ pages/
│  ├─ ai-governance.tsx
│  ├─ ai-search-llmo.tsx
│  ├─ Blog.tsx
│  ├─ BlogPost.tsx
│  ├─ for-construction.tsx
│  ├─ home.tsx
│  ├─ not-found.tsx
│  ├─ president-excel.tsx   ← 新規
│  └─ privacy.tsx
```

トップページのカードは、カードが2件のみ・他で再利用しないことを踏まえ、`home.tsx` 内で直接実装するか、既存 `components` 配下に共通カード部品を追加するかのどちらか。過剰な分割は避ける。

---

## president-excel 詳細ページ

`/president-excel` は改善モデル01の詳細説明ページ。見積システムを商品として売るページにはせず、NDLの改善方法を理解してもらうモデルページとして構成する。

### 1. Hero
- ラベル：`改善モデル 01`
- 見出し：`社長だけが扱える見積Excelを、会社の資産へ変える。`
- 導入文：
```
長年使われてきた見積Excelには、
単価、判断基準、例外対応、現場の経験が蓄積されています。

NDLは、それを否定して作り直すのではなく、
中にある知恵を整理し、チームで使え、
次の世代へ残せる仕組みへ育てます。
```
（既存LPの文章トーンに合わせて調整可）

### 2. 現状の課題
```
複雑な関数が絡み合い、全体を把握できる人が限られている
見積の作成や修正が、社長や特定担当者に集中している
単価や材料情報が、複数のシートやファイルに散らばっている
難工事や例外対応の判断理由が記録されず、毎回ゼロから判断している
```

### 3. NDLの改善ステップ
```
01 整理する
業務の流れ、入力項目、計算ロジック、単価情報を見える化します。

02 共有する
特定の人だけが扱っていた仕組みを、チームで使える状態に整えます。

03 継承する
判断理由や例外対応を蓄積し、将来の検索やAI活用につなげます。
```
いきなりAI化するのではなく、段階的に改善するNDLの思想が伝わるように。

### 4. 目指す状態
```
入力、計算、単価情報の役割が整理され、仕組みを理解しやすくなる
チームで見積を作成・確認し、必要な判断だけを社長が行える
単価、材料、過去案件を一元的に参照できる
例外対応の理由が蓄積され、類似案件の判断に再利用できる
```

### 5. 将来のAI活用
AIは最終段階の補助として位置づけ、強く売り込まない。
```
判断理由や例外対応が蓄積されれば、
将来的には過去の類似案件を検索したり、
見積の下書きや判断補助にAIを使えるようになります。
```
「AIが社長の判断を完全に代行する」等の断定表現は避ける。

### 6. 結論
```
ツールを入れることが、ゴールではありません。

社長の経験を奪うのではなく、
会社の中で使い続けられる形へ変えることが目的です。
```

### 7. CTA
既存LPのCTA設計・既存の問い合わせ導線にそのまま接続。新しいCTAデザインは作らない。

---

## デザイン方針

**トップページ**：Hero/Challenges/Approach/Philosophyのセクション幅・余白・角丸・枠線・影・見出しサイズ・本文サイズ・色・ホバー・アニメーションを統一。

**詳細ページ**：`for-construction.tsx` / `ai-governance.tsx` / `ai-search-llmo.tsx` を参考にするが、デザインをそのままコピーせず、既存サイト全体の共通トーンへ合わせる。

## アニメーション

既存の実装方式を再利用。新規ライブラリ追加は不可。トップページのカードはスクロール進入時に軽くフェードアップ（PCはわずかな時間差可、スマホは上から自然に表示）。禁止：強いバウンド、大きな拡大縮小、常時ループ、高速移動、点滅、読解を妨げる演出。`prefers-reduced-motion` に対応。

## ルーティング

`/president-excel` `/for-construction` を追加・確認。既存のルーティング方式に合わせる。新しいルーターや依存パッケージは導入しない。

## 変更してはいけない部分

Hero／Challenges／Approach／Philosophy／ヘッダー／ナビゲーション項目／既存リンク／フッター／SEOメタ情報／構造化データ／既存ページ全体のデザイン／既存の `for-construction.tsx` の内容。

**主な変更対象**：`src/pages/home.tsx`、`src/pages/president-excel.tsx`（新規）、ルーティング定義、必要に応じた既存 `components`。

---

## 完成条件

**トップページ**：親ラベルが「改善モデル」／「改善モデル01」「改善モデル02」が表示される／2つの改善モデルの違いが一目で分かる／見積業務だけを扱う会社に見えない／詳細説明を詰め込みすぎていない／`/president-excel` と `/for-construction` へ正しく遷移できる／PCは2カラム・スマホは縦並び／横スクロールが発生しない

**president-excelページ**：見積Excelの属人化課題が具体的に分かる／「整理→共有→継承」が伝わる／AI導入を急がない姿勢が伝わる／見積システムの販売ページに見えない／既存サイトとデザインが統一されている／問い合わせ導線がある／コンソールエラーがない／既存ページへ影響を与えていない

---

## 実装後の報告フォーマット

```
1. 追加したファイル
2. 変更したファイル
3. 削除または置換した既存要素
4. トップページに実装した内容
5. president-excelページに実装した内容
6. ルーティング変更
7. PCでの確認結果
8. スマートフォンでの確認結果
9. 既存ページへの影響
10. 残っている調整事項
```
